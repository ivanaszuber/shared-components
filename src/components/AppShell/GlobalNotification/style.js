import styled from 'styled-components';
import { white, blueDarker } from '../../style/colors';

export const GlobalNotificationStyled = styled.div`
  width: 100%;
  background-color: ${blueDarker};
  padding: 16px;
  text-align: center;
  color: ${white};
  font-weight: bold;
`;